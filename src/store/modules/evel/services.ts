import ApiConfig from "../../../config/api-config";
import { $apiClient } from "../../../utils/apiClient";
import { EvelStatePayload } from "./types";

export function getDataEvel(payload:EvelStatePayload) {
  // const url = `${ApiConfig.EVEl}`;
  // // console.log(url);
  // return $apiClient.get(url);
}
